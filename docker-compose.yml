services:
  api:
    build: .
    container_name: ai-video-factory-v3
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./media:/app/media
      - ./data:/app/data
    restart: unless-stopped
